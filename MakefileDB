CXX = g++
CXXFLAGS = -std=c++17
objects =  UnitTestFramework.o date.o database.o main.o condition_parser.o condition_parser_test.o node.o token.o 

all: $(objects)
	$(CXX) -o $@ $(CXXFLAGS) $(objects) $(LDFLAGS) $(LDLIBS)

$(objects): %.o: %.cpp %.h

clean: 
	rm *.o && rm all
